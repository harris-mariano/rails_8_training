<h1>Articles</h1>

<ul class="list-group">
  <% @articles.each do |article| %>
    <li class="list-group-item">
      <%= link_to "#{article.title} - #{article.user.first_name} #{article.user.last_name}", article %>
    </li>
  <% end %>
</ul>